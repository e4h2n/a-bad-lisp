(
  let factorialize (
    lambda factorial (
      lambda n (
        if n
        (* n (factorial (+ n -1)))
        1
      )
    )
  )

  (lambda f (f f))
  (lambda recur (factorialize ((lambda x) ((recur recur) x))))

  10
)
