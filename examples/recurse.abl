(
  let fibbonize (
    lambda fib (
      lambda n (
        if (* n (+ n -1))
        (+ (fib (+ n -1)) (fib (+ n -2)))
        n
      )
    )
  )

  (
    (lambda f (f f))
    (lambda recur (
      fibbonize (lambda x (recur recur x))
    ))

    10
  )
)
