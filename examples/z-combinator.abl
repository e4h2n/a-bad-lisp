(
	let Z (
		lambda F (
			(lambda f (f f))
			(lambda recur (
				F (lambda x (recur recur x))
			))
		)
	) (
	let fibonacci(
		Z (lambda help (
		  	lambda n (
		  		if (* n (+ n -1))
		  		(+ (help (+ n -2)) (help (+ n -1)))
		  		n
			  )
		  )
		)
	)
	(fibonacci 10)
	)
)
